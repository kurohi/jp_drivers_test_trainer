# JP Drivers Test Trainer

Japanese driver's license test trainer — monorepo with FastAPI backend + React frontend.

## Quick Start

```bash
# Backend
cd backend
uv sync
uv run uvicorn src.main:app --reload --port 8000

# Frontend
cd frontend
npm install
npm run dev
```